server {
    # listen 80;
    # location ^~ /uploads/ {
    #     root /home/box/web/;
    # }
    # location ~* ^.+ {
    #     root /home/box/web/public;
    # }
    location /hello/ {
        127.0.0.1:8080
    }
}
